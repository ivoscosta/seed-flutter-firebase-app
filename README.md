# seed-flutter-firebase-app
A seed Flutter project that already have the following resources:

Flutter modular;
Routes;
Dialogs;
i18n;
globals;
theme;
colors;
dimensions;
fonts;
assets;
utils;
Firebase;
Auth with social;
